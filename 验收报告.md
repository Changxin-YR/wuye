# 美家物业 V2 最终验收报告

## 交付范围

当前目录包含完整 V2 源码、Jinja2 页面、静态资源、测试、Agent OpenAPI 配置和由模型元数据生成的 `schema.sql`。保留 Flask、Jinja2、SQLAlchemy、MySQL 技术栈，AI 上游改为阿里云百炼 OpenAI 兼容 API。

## 本轮修复

- 修复生产模式发现未迁移数据库时 Engine 未释放的问题，Windows SQLite 临时文件可正常清理。
- 以当前 `models.py` 重新生成 `schema.sql`：34 张表、外键、唯一/检查约束、索引、V2 迁移标记、默认小区及完整内置岗位/权限数据。
- 更新 `README.md`、`docs/开发规格说明书.md` 与本报告，使模块、权限、Agent、迁移和测试数据与 V2 代码一致。
- 复核 `/houses`、`/users`、`/notices`、`/orders`、`/audit`：读取经过范围过滤，写入经过业务 Service；无前端身份字段授权，无直接 ORM/SQL Agent 写入。
- 百炼 `/ai/chat` 支持 `stream=true` 的 SSE 增量输出；默认 JSON 响应保持兼容。工具调用分片会在服务端合并，重复工具调用会被阻断。

## 自动测试

执行：

```text
python -m unittest discover -s tests -v
```

结果：`Ran 131 tests ... OK`。

覆盖内容包括：

- 房屋/楼栋/单元、人员关系、租赁历史和有效关系授权。
- 工单派单、改派、接单、进度、完工、返修、验收、取消和评价。
- 投诉、访客、车辆、车位、设备、巡检及财务账单、批量账单、收款、冲销。
- RBAC、社区/楼栋范围、会话失效、CSRF、密码锁定、乐观锁、幂等和失败回滚。
- Agent 查询/普通执行/高风险确认、令牌归属、版本复核和审计。
- 百炼 OpenAI 兼容 HTTP、鉴权、超时、限流和错误处理；初始化、升级、幂等迁移及 MySQL DDL 编译。

## 真实环境命令

项目 `.env` 已切换为百炼配置模板；填入真实 `BAILIAN_API_KEY` 后执行：

```powershell
python manage.py upgrade-db
python manage.py diagnose --ai --infer
```

`upgrade-db` 只做增量迁移并保留现有数据；`diagnose --ai --infer` 检查数据库、schema 和百炼模型推理链，不输出密钥。Agent 工具定义文件为 `config/dify_agent_openapi.json`。

## 百炼配置状态

`config/dify_agent_openapi.json` 已通过 JSON 校验。Key 加载顺序为 `BAILIAN_API_KEY`，为空时回退 `DASHSCOPE_API_KEY`；不会把 Dify 密钥发送到百炼。当前部署环境使用 `DASHSCOPE_API_KEY`，`python manage.py diagnose --ai --infer` 已返回数据库、schema 和百炼推理均正常。

## 安全验收结论

- 身份只来自服务端登录会话对应的数据库用户；不信任 `user_id`、`role` 等前端/模型字段。
- 普通业务可直接执行；删除、解绑、退租、权限修改、收费冲销等高风险操作需要二次确认。
- Agent 只能调用业务 Service；查询和写入均复核当前权限、数据范围、状态、版本与幂等键。
- 附件按工单/用户归属鉴权并重新编码，审计快照排除密码、令牌等敏感字段。
